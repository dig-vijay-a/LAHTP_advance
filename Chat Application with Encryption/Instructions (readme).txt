Required modules should be installed

Modules:- socket, threading, cryptography

This directory contains the server and client code for the application, many client can enter the application and broadcast their message securely.

The code uses unique key generated by fernet tool of cryptography module,

after running the server file the output displays a generated key, it should be pasted in the client file encryption_key variable then the connection will be established.